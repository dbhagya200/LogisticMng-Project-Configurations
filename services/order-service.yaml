spring:
  mvc:
    problemdetails:
      enabled: true

  data:
    mongodb:
      host: localhost
      port: 13500
      database: logistic
      username: root
      password: mongodb
      authentication-database: admin

server:
  port: 8001

eureka:
  instance:
    hostname: localhost
    prefer-ip-address: false
  client:
    serviceUrl:
      defaultZone: http://localhost:9001/eureka/